สำหรับคนที่สนใจรันโค้ดให้ติดตั้งดังนี้
1.ลง Python เวอร์ชันล่าสุด https://www.python.org/downloads/ ระหว่างการติดตั้ง ติ๊กถูกช่อง "Add Python to PATH" แล้วกด Install
2. pip install --upgrade pip
3. pip install -r requirements.txt
4. จากนั้นรันด้วยคำสั่ง python ex01.py
